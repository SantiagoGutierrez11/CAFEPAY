/* CREACION DEL USUARIO

ALTER SESSION SET "_ORACLE_SCRIPT"=TRUE;
DROP USER adminCAFEPAY;
CREATE USER adminCAFEPAY IDENTIFIED BY adminCAFEPAY;
GRANT DBA, CONNECT TO adminCAFEPAY;

*/

--------------------------------------------------------
--  Eliminación previa (si existe)
--------------------------------------------------------
DROP TABLE COLLECTOR CASCADE CONSTRAINTS;
DROP TABLE STATUS_DOMAIN CASCADE CONSTRAINTS;

--------------------------------------------------------
--  Tabla de dominio estático para el estado del recolector
--------------------------------------------------------
CREATE TABLE STATUS_DOMAIN (
    STATUS_ID NUMBER NOT NULL,
    STATUS_NAME VARCHAR2(20) NOT NULL
);

--------------------------------------------------------
--  Datos estáticos del dominio de estado
--------------------------------------------------------
INSERT INTO STATUS_DOMAIN (STATUS_ID, STATUS_NAME) VALUES (1, 'ACTIVE');
INSERT INTO STATUS_DOMAIN (STATUS_ID, STATUS_NAME) VALUES (2, 'INACTIVE');

--------------------------------------------------------
--  Creación de tabla COLLECTOR
--------------------------------------------------------
CREATE TABLE COLLECTOR (
    WORKER_CODE       VARCHAR2(30) NOT NULL,  -- código único de trabajador, clave primaria
    ID                VARCHAR2(30) NOT NULL,        -- FK a PERSON (si existe)
    FIRST_NAME        VARCHAR2(50) NOT NULL,
    LAST_NAME         VARCHAR2(50) NOT NULL,
    PHONE             VARCHAR2(20) NOT NULL,
    STATUS_ID         NUMBER NOT NULL         -- FK a STATUS_DOMAIN
);

--------------------------------------------------------
--  Definición de restricciones (Constraints)
--------------------------------------------------------

--------------------------------------------------------
--  Collector
--------------------------------------------------------
-- Clave primaria
ALTER TABLE COLLECTOR 
    ADD CONSTRAINT COLLECTOR_PK PRIMARY KEY (WORKER_CODE) ENABLE;
ALTER TABLE STATUS_DOMAIN 
    ADD CONSTRAINT STATUS_PK PRIMARY KEY (STATUS_ID) ENABLE;

-- Clave única (para el id personal)
ALTER TABLE COLLECTOR 
    ADD CONSTRAINT COLLECTOR_UQ_ID UNIQUE (ID) ENABLE;

-- Llave foránea a la tabla de dominio STATUS_DOMAIN
ALTER TABLE COLLECTOR 
    ADD CONSTRAINT COLLECTOR_FK_STATUS 
    FOREIGN KEY (STATUS_ID) 
    REFERENCES STATUS_DOMAIN (STATUS_ID) ENABLE;
-- Check de status
ALTER TABLE COLLECTOR 
  ADD CONSTRAINT COLLECTOR_CHK_STATUS CHECK (STATUS_ID IN (1,2)) ENABLE;
--------------------------------------------------------


/*
INSERT
*/
INSERT INTO COLLECTOR (WORKER_CODE, ID, FIRST_NAME, LAST_NAME, PHONE, STATUS_ID)
VALUES ('W0001', 'C0001', 'Juan',    'Pérez',    '3001234567', 1); -- Activo

INSERT INTO COLLECTOR (WORKER_CODE, ID, FIRST_NAME, LAST_NAME, PHONE, STATUS_ID)
VALUES ('W0002', 'C0002', 'María',   'Gómez',    '3017654321', 2); -- Inactivo

INSERT INTO COLLECTOR (WORKER_CODE, ID, FIRST_NAME, LAST_NAME, PHONE, STATUS_ID)
VALUES ('W0003', 'C0003', 'Andrés',  'López',    '3025550000', 1); -- Activo

INSERT INTO COLLECTOR (WORKER_CODE, ID, FIRST_NAME, LAST_NAME, PHONE, STATUS_ID)
VALUES ('W0004', 'C0004', 'Luisa',   'Martínez', '3034441111', 1); -- Activo

INSERT INTO COLLECTOR (WORKER_CODE, ID, FIRST_NAME, LAST_NAME, PHONE, STATUS_ID)
VALUES ('W0005', 'C0005', 'Carlos',  'Rojas',    '3043332222', 2); -- Inactivo

COMMIT;


